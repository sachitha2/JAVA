import javax.swing.*;
import java.awt.*;

public class Secound extends JFrame {
    JLabel label;
    Secound(){
        label = new JLabel("My secound ");
        add(label);

        setLayout(new FlowLayout());
    }
}
